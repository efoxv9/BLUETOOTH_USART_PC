# BLUETOOTH_USART_PC
USB–USART Keyboard Transponder — A desktop utility that forwards keystrokes to a serial port in real time. Type in the window; each character is transmitted byte-by-byte over USART immediately. Doubles as a serial debug terminal with HEX/ASCII display.
